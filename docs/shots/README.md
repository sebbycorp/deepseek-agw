# Screenshots / GIFs

Drop the standalone clips here. Reserved names:

| File | What |
| --- | --- |
| `harness-settings.png` | Harness http://127.0.0.1:3080 — Settings → Models, `agw` |
| `agw-ui.png` | agentgateway admin http://127.0.0.1:14010/ui |
| `harness-run.gif` | New Session, pick `agw` / `gpt-4o`, a turn |
| `agw-costs.gif` | http://127.0.0.1:14010/ui/llm/costs |

Nothing here yet. Nothing here is a cluster screenshot, a real API key, or a claim that MCP is wired.
